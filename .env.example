# Copy this file to .env before starting: cp .env.example .env

# Local development port mapping configuration
# External port mapping for browser access (http://localhost:8080)
REVERSE_PROXY_PORT=8080
# Internal port for the backend
API_CORE_PORT=8081
# Internal port for the frontend
WEB_CLIENT_PORT=8080
