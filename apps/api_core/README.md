# API Core

Core backend service for the Echoback project

## Purpose
Acts as the central business logic controller, manages user profiles,
handles user authentication, coordinates email delivery flows, and owns the PostgreSQL database
